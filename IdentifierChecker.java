public class IdentifierChecker {
  public static boolean isValid(String idn) {
    try{
      // the other if's for checking if it is
      // a letter, another from the underscore
      // and another dolar sign
      Charater.parseInt(idn[0])
      return true;
    }except(){
      return false;
    }
  }
}
